# 安全风险与对策

## Sybil攻击

**风险：** 攻击者创建大量虚假节点占据网络。
**对策：**
- 轻量工作量证明：
    - 接受连入的节点提供一个哈希前置0位长度和一个随机数（16字节）。
    - 对端计算并提供一个大于该前置0位数的哈希值和哈希源，哈希源的末尾为该随机数。
    - 对端可用的哈希源的长度为32字节。
    - 因此哈希源的总长为48字节，计算量适中。**注**：哈希算法为 `SHA-256`。
- 节点信誉系统（新节点权重低）。
- IP分布检测（同一IP段节点数限制）。


## 日蚀攻击

**风险：** 攻击者隔离目标节点，使其只连接到恶意节点
**对策：**
- 候选池多样性(不同IP段、不同地区)。
- 随机连接策略。
- 定期刷新连接。
- 多源节点信息交叉验证（**注**：仅适用于请求发起者，不适用中转节点）。


## DDoS放大攻击

**风险：** 搜寻机制被用于DDoS放大攻击
**对策：** 因为对节点的搜寻只是为了找到入口，无需高频。所以搜寻速率有限制，策略如下：
- 对于同一个名识的请求，需最少间隔 `30秒` 才能再次发送。
- 对于不同名识的请求，发送频率为 `1秒` 间隔。且一个大间隔（30秒）内最多允许 `15次` 请求。

注意速率限制并不是由节点自我约束，而是上级节点强限制，因为超出的请求会被丢弃。

考虑一定的宽容性，发送请求的节点可能需要设置稍高一些的间隔，比如相同名识的请求按 `32~33秒` 间隔，不同名识的请求按 `1.5~2秒` 的间隔发送，以避免请求被丢弃。

> **提示：**
> 作为发出请求的客户端，可以同时向多个节点请求搜寻。


## 流量分析攻击

**风险：** 攻击者通过流量模式识别网络
**对策：**
- 随机延时（破坏时序特征）。
- 流量填充（破坏包大小特征）。
- 时序混淆（随机消息顺序）。
- 协议伪装（HTTP/2等常见协议）。


## 服务劫持

**风险：** 恶意节点伪装成服务提供者
**对策：**
- 服务质量评估（低质量服务被淘汰）。
- 多节点验证（从多个服务节点获取结果，适用于初始请求发起者）。
- 服务响应验证（验证服务返回的正确性）。
- 黑名单机制（恶意服务提供者被封禁）。


## 资源耗竭攻击

**风险：** 恶意节点大量消耗网络资源
**对策：**
- 连接数限制（每节点最大连接数）。
- 带宽限制（每连接最大带宽）。
- 请求频率限制（防止请求洪泛）。
- 快速隔离机制（异常节点快速封禁）。
